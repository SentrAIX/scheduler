{
  "name": "sentraix-scheduler",
  "version": "0.1.0",
  "description": "Lightweight scheduler to call SentrAIX API endpoints (billing, runs).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo \"no build step for scheduler\""
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.0",
    "p-retry": "^5.0.0"
  }
}